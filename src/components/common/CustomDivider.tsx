import { Divider } from "@mui/material";

const CustomDivider: React.FC = () => {
  return (
    <Divider sx={{ margin: "1rem 0", borderColor: "rgba(0, 0, 0, 0.20)" }} />
  );
};

export default CustomDivider;
